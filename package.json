{
  "name": "@apex20/monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Apex20 - High Performance Virtual Tabletop",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "test": "turbo test",
    "type-check": "turbo type-check"
  },
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.3.2",
  "devDependencies": {
    "turbo": "latest"
  }
}
