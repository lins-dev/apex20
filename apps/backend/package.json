{
  "name": "@apex20/backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "go run cmd/api/main.go",
    "build": "go build -o bin/api cmd/api/main.go",
    "test": "go test ./...",
    "lint": "golangci-lint run"
  }
}
